import styled from "@emotion/styled";
import { Box } from "@mui/system";

export const StyledInfoType = styled(Box, { shouldForwardProp: () => true })({
  fontFamily: "Heuvetica Neue",
  color: "#555",
  //   width: "30px",
  //   marginRight: 40,
});
